services:
  diun:
    image: crazymax/diun:${DIUN_VERSION}
    command: serve
    volumes:
      - "/opt/docker/privileged/diun:/data"
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    env_file: .env
    labels:
      - "diun.enable=true"
    restart: always
