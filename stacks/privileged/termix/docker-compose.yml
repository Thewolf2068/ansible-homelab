services:
  termix:
    image: bugattiguy527/termix:latest
    container_name: termix
    restart: unless-stopped
    ports:
      - '1080:8080'
    volumes:
      - /opt/docker/privileged/termix:/app/data
    environment:
      PORT: '8080'

volumes:
  termix-data:
    driver: local
